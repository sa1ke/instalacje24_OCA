# UX_IMPROVEMENTS (Android Tablet 10")

Wprowadzone usprawnienia w `instalacje24_terrain`:

- Duże przyciski 64px (`START`, `STOP`, `Faktura 1 klik`, `Zakończ zlecenie`).
- Układ dwukolumnowy na formularzu zlecenia (`inst24-grid`).
- Sticky header dla najważniejszych akcji.
- Pływający przycisk zakończenia zlecenia.
- Klikalny numer telefonu (`widget="phone"`).
- Szybkie przyciski Google Maps + trasa.
- Upload zdjęć przygotowany pod mobilną kamerę (`image/*`).
- Placeholder voice-to-text.
- Dark mode CSS (`prefers-color-scheme: dark`).
- Wskaźnik offline + retry sync (placeholder UI + modele bufora).
- Bufor ostatnich danych przez modele szkiców offline.

Cel: minimalna liczba kliknięć i szybka obsługa w terenie.
